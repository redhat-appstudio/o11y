apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: rhtap-kanary-alerting-rules
  labels:
    tenant: rhtap
spec:
  groups:
  - name: kanary_alerts
    interval: 1m
    rules: []
