---
# ServiceAccount + Secret for Prometheus to scrape the secured /metrics endpoint
apiVersion: v1
kind: ServiceAccount
metadata:
  name: kaexporter-metrics-reader
  namespace: konflux-o11y-exporters
---
apiVersion: v1
kind: Secret
metadata:
  name: kaexporter-metrics-reader
  namespace: konflux-o11y-exporters
  annotations:
    kubernetes.io/service-account.name: kaexporter-metrics-reader
type: kubernetes.io/service-account-token
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: kaexporter-metrics-reader
rules:
  - nonResourceURLs:
      - /metrics
    verbs:
      - get
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: kaexporter-metrics-reader-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: kaexporter-metrics-reader
subjects:
  - kind: ServiceAccount
    name: kaexporter-metrics-reader
    namespace: konflux-o11y-exporters
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: kaexporter-servicemonitor
  namespace: konflux-o11y-exporters
spec:
  endpoints:
    - path: /metrics
      port: https
      scheme: https
      bearerTokenSecret:
        name: kaexporter-metrics-reader
        key: token
      tlsConfig:
        insecureSkipVerify: true
      interval: 300s
      scrapeTimeout: 180s
  selector:
    matchLabels:
      app: kaexporter
